# Least Recently Used Cache

An LRU cache is a type of cache that holds onto the most frequently used items. When a new item is added and 
the cache is already at maximum capacity, it removes the least recently used item in the list, hence the name 
LRU cache.